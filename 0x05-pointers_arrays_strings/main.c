#include <stdio.h>
#include "main.h"
/**
 * main -Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char s[] = "Nafiu Ahmad";

	printf("%d is the length of the string you sent", _strlen(s));
}
