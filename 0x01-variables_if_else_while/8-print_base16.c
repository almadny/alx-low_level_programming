#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print hexadecimal number
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 15; ++a)
	{
		putchar(a);
	}

	putchar('\n')
	return (0);
}
