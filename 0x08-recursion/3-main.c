#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 **/

int main(void)
{
	printf("The factorial of 6 is %d\n",factorial(6));
	printf("The factorial of 0 is %d\n",factorial(0));
	printf("The factorial of -12 is %d\n",factorial(-12));
	return (0);
}
